#pragma once
// #ifdef MATH
// #define MATH

class Math{

public:
    static int add(int , int);
    static int mul(int , int);
    static float divd(int , int);
    static int sub(int , int);
};

// #endif